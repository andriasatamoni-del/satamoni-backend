# نظام الطباعة الإنتاجي — تقرير التسليم (A–K)

**الحالة النهائية: الكود مكتمل ومُختبر بالكامل على مستوى الباك إند (25/25 اختبار Jest جديد + 668/668
إجمالي بعد الدمج). الطباعة الفعلية على الطابعة الحرارية الحقيقية (XP-D200N) لسه محتاجة تحقق يدوي من
جهازك - راجع قسم K تحت للإجراء بالظبط. الالتزام صريح: النظام ده مش هيتوصف "جاهز للإنتاج" لحد ما تتأكد
بنفسك إن الإيصالات/التذاكر الستة طبعت فعليًا صح وبالتوجيه الصح ومن غير تكرار.**

---

## A) نظرة عامة على المعمارية

```
POS/Order Workflow  →  Database (print_jobs, داخل نفس transaction الطلب)
                     →  Branch Print Agent (Node.js منفصل، خارج transaction الطلب تمامًا)
                     →  الطابعة الحرارية الفعلية (ESC/POS 80mm)
```

القاعدة اللي كل قرار تصميم اتاخد على أساسها: **فشل الطباعة ميوقفش أو يلغي طلب/دفع أبدًا.** اتحقق منها
فعليًا باختبار حي (`tests/printing.test.js` - "طلب تيك أواي من غير أي توجيه متظبط") بيتأكد إن الطلب
بينجح (201) حتى في فرع من غير أي طابعة أو محطة متظبطة خالص.

تفاصيل كاملة في `docs/PRINTING-SYSTEM.md`.

## B) نموذج البيانات

جداول جديدة: `printers`، `kitchen_stations`، `print_jobs`. أعمدة جديدة: `menu_categories.station_id`،
`menu_items.station_id`. مهاجرة عبر `db/migrations/0015_printing.js` (متحقق منها فعليًا ضد: تثبيت جديد
بالكامل، قاعدة بيانات موجودة من قبل الطباعة، وإعادة تطبيق مباشرة - الثلاثة نجحوا بدون أخطاء). نفس
التغييرات مضافة لـ`db/schema.sql` (مسار التثبيت الجديد).

## C) مرجع الـAPI

- `routes/printers.js` — إدارة الطابعات (CRUD + اختبار طباعة).
- `routes/kitchen-stations.js` — محطات التحضير + توجيه المنيو (قسم/صنف -> محطة).
- `routes/print-jobs.js` — واجهة الـAgent فقط (claim/printed/failed/retry) - مفيش أي وصول مباشر لقاعدة
  البيانات من الـAgent خالص، كل حاجة عن طريق الـHTTP API ده بحساب مستخدم حقيقي عادي.

## D) التوجيه والحدث المُطلق للطباعة

جدول كامل بكل نوع طلب/حدث/مستندات في `docs/PRINTING-SYSTEM.md`. ملخص:
- **تيك أواي**: كل حاجة (إيصال + ملخص مطبخ + تذاكر المحطات) عند تأكيد الطلب مباشرة.
- **دليفري**: ملخص + تذاكر عند الإنشاء، الإيصال النهائي (فيه سعر) عند تسليم الطلب فعليًا للسائق.
- **صالة**: مفيش طباعة عند الإنشاء خالص - تذاكر المطبخ لما الحالة توصل PREPARING فعليًا، والفاتورة بطلب
  الجرسون (idempotent - مفيش تكرار).

## E) قواعد المحتوى (فيه سعر / من غير سعر)

`CUSTOMER_RECEIPT`، `DELIVERY_FINAL_RECEIPT`، `DINE_IN_BILL` = فيها سعر (بتتسلّم للعميل).
`KITCHEN_TICKET`، `KITCHEN_SUMMARY`، `DELIVERY_SUMMARY` = من غير سعر خالص (للمطبخ/السائق بس). كل ده
متحقق منه باختبارات صريحة (`db/print-templates.js::money()` موجودة أو مش موجودة في `content_html`).

## F) الـIdempotency

`print_jobs.idempotency_key` (UNIQUE) + `ON CONFLICT DO NOTHING` - نفس النمط المستخدم أصلًا في
`db/accounting-engine.js` و`db/inventory-ledger.js`. اتحقق منه فعليًا: فاتورة الصالة (ضغطتين متتاليتين
على "اطبع الفاتورة" بيرجّعوا نفس صف `print_jobs.id` بالظبط، مفيش صف تاني)، وclaim مزدوج على نفس الأمر
(409 - مفيش طباعة مزدوجة من غير قصد لو الـAgent اتشغّل مرتين بالغلط).

## G) واجهة الإدارة

`public/satamoni-printing.html` (تسجيل دخول admin/branch_manager). ٣ تبويبات: الطابعات (CRUD + تفعيل/
تعطيل + افتراضية للنوع + اختبار طباعة)، المحطات والتوجيه (محطات + ربط كل صنف/قسم بمحطة)، طابور الطباعة
(مراقبة الحالة + إعادة محاولة يدوية للأوامر الفاشلة).

## H) Branch Print Agent

مشروع Node.js مستقل تمامًا في `print-agent/` - **خارج الباك إند عمدًا** (مايكسرش ديبلوي Linux/Render
بمكتبات Windows). Puppeteer (نسخة Chromium معزولة) + `pdf-to-printer` (طباعة صامتة، من غير أي نافذة/
dialog - بيتفادى المشكلة اللي واجهناها فعليًا مع `--kiosk-printing` وChrome العادي). تعليمات التركيب
والتشغيل الدائم في `print-agent/README.md`.

## I) التغطية بالاختبارات

`tests/printing.test.js` - 25 اختبار حي ضد Postgres حقيقي (مش mocks): CRUD الطابعات/المحطات + صلاحيات +
عزل فروع، توليد الأنواع الستة الصحيحة لكل نوع طلب في اللحظة الصحيحة، فشل التوجيه بدون توقف الطلب، تفكيك
الكومبو لمحطتين منفصلتين، منع تكرار فاتورة الصالة، ودورة حياة الـAgent API كاملة (claim/printed/failed/
retry) بما فيها منع claim مزدوج. **٦٦٨/٦٦٨ اختبار (كل الاختبارات القديمة + الجديدة) شغالة بنجاح بعد
الدمج** - مفيش أي كسر لأي وظيفة موجودة (طلبات/مخزون/محاسبة/HR/رواتب/دفع).

## J) قيود صريحة (مش مخفية)

1. **LAN (شبكة) مش مفعّلة فعليًا** - جاهزة معماريًا في الـschema/الواجهة، الـAgent حاليًا USB بس (راجع
   `docs/PRINTING-SYSTEM.md` لسبب التفصيل - طباعة LAN حقيقية محتاجة مسار رندر ESC/POS خام مختلف تمامًا).
2. **مفهوم "حساب مفتوح للصالة" مش موجود في النظام أصلًا** (ولا اتضاف هنا - عمدًا، زي ما اتطلب صراحة "متتعملش
   نظام طلبات تاني متعارض"). طلب الصالة بيتسجل مدفوع من لحظة الإنشاء زي التيك أواي بالظبط؛ `DINE_IN_BILL`
   بيتطبع بطلب الجرسون في أي وقت قبل الإلغاء - وده اللي بيعادل "الفاتورة النهائية" في السياق الحالي.
3. **الطباعة الفعلية على XP-D200N لسه مش اتأكدت من جهاز حقيقي** - أنا (Claude) شغال في بيئة sandboxed
   من غير أي وصول لطابعة فعلية أو جهاز Windows حقيقي، فمفيش طريقة أتحقق بيها من الطباعة الفعلية بنفسي.
   كل الـ25 اختبار بيتحققوا من الطبقة اللي قبل الورقة نفسها (الأمر، التوجيه، المحتوى، الـAPI) - مش من
   خروج الورقة فعليًا من الطابعة.

## K) الإجراء اليدوي بالضبط على XP-D200N (المطلوب منك تنفيذه)

1. **شغّل السيرفر والداتابيز** (نفس اللي بتشتغل بيه دلوقتي)، وشغّل migration 0015 لو مطبقتش لسه
   (بيتشغل أوتوماتيك مع بدء تشغيل السيرفر - راجع `db/migrate.js`).
2. **من `satamoni-printing.html`**: سجّل طابعتك (XP-80C) - النوع CASHIER، اسم النظام بالظبط زي ما هو في
   Windows، وحطها افتراضية للنوع. اعمل محطة مطبخ واحدة (أو أكتر لو عندك بيتزا/حلواني منفصلين) واربطها
   بطابعة (ممكن نفس الطابعة لو عندك واحدة بس دلوقتي)، واربط أصناف المنيو بالمحطات المناسبة.
3. **جهّز الـAgent** على نفس جهاز الكاشير (راجع `print-agent/README.md` بالتفصيل): `npm install`،
   `.env` بالإعدادات الصحيحة (رابط السيرفر، رقم الفرع، حساب دخول)، `npm start`.
4. **اضغط "اختبار طباعة"** من شاشة الطابعات - تأكد إن ورقة تجريبية طلعت فعليًا من XP-D200N بشكل صحيح.
5. **نفّذ الـ6 سيناريوهات دي بالترتيب وتأكد كل واحدة طبعت صح، بدون تكرار، وعلى الطابعة/المحطة الصحيحة:**
   - طلب تيك أواي حقيقي → إيصال العميل يطبع فورًا.
   - نفس الطلب لو فيه صنف بيتزا → تذكرة مطبخ بيتزا منفصلة تطبع فورًا كمان.
   - طلب فيه صنف حلواني (لو عندك محطة حلواني منفصلة) → تذكرة حلواني منفصلة عن تذكرة البيتزا.
   - طلب دليفري → ملخص دليفري يطبع فورًا (من غير سعر)، وبعد ما تعيّن سائق وتضغط "خرج للتوصيل" → إيصال
     دليفري نهائي منفصل (فيه سعر) يطبع.
   - طلب صالة → لما تقدّم حالة المطبخ لـ"PREPARING" من شاشة الكاشير/KDS → تذكرة مطبخ تطبع. اضغط "اطبع
     الفاتورة" مرتين على نفس الطلب → تأكد إنها طبعت مرة واحدة بس، مش نسختين.
6. **لو أي خطوة فشلت**: راجع تبويب "طابور الطباعة" في `satamoni-printing.html` - هتلاقي سبب الفشل
   واضح (طابعة مقطوعة، اسم غلط، توجيه ناقص). ده جزء من التصميم عمدًا - مفيش أمر طباعة بيختفي بصمت.

بعد ما تتأكد من الـ6 سيناريوهات دي فعليًا على جهازك، النظام يبقى جاهز للإنتاج بمعنى الكلمة الكامل.
